{
  "name": "kotrol",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --dev"
  },
  "dependencies": {
    "preact": "^10.29.7"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
