{
  "name": "kotrol",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --dev"
  },
  "dependencies": {
    "preact": "^10.28.4"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
